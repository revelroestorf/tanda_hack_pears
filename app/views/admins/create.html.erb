<p id="notice"><%= notice %></p>

<table>
  <tr>
    <td>User1</td>
    <td></td>
    <td>User2</td>
  </tr>


<% Pairing.all.each do |pair| %>

<tr>
  <td><%= User.find(pair.user1).name %></td>
  <td> - </td>
  <td><%= User.find(pair.user2).name %></td>
</tr>
<% end %>

</table>

<br>
<br>
<br>
<%= link_to 'Delete Pairings', {controller: 'admins', action: 'destroy'}, method: 'delete' %>
